const userDB = require("./db-data.js");

async function getUsers(req, res) {
    console.log("Attempting to get other users from this userMai");
        let userMail = req.body['mail'];
};

module.exports = getUsers;